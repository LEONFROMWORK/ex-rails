<div class="p-6">
  <h1 class="text-3xl font-bold mb-6">Vue.js Test Page</h1>
  
  <div id="test-vue-app" class="border p-4 rounded">
    <p>If Vue is working, this text will be replaced.</p>
  </div>
  
  <script type="module">
    import { createApp } from 'vue'
    
    const TestApp = {
      template: `
        <div class="text-center">
          <h2 class="text-2xl text-green-600 font-bold mb-4">✅ Vue.js is working!</h2>
          <p class="text-gray-600">{{ message }}</p>
          <button @click="count++" class="mt-4 px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
            Count: {{ count }}
          </button>
        </div>
      `,
      data() {
        return {
          message: 'Vue + shadcn-vue integration successful!',
          count: 0
        }
      }
    }
    
    createApp(TestApp).mount('#test-vue-app')
  </script>
</div>