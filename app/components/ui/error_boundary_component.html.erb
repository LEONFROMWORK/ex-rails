<div class="<%= container_classes %>" role="alert" aria-live="assertive">
  <div class="flex items-start">
    <div class="flex-shrink-0">
      <svg class="w-5 h-5 text-red-600 dark:text-red-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
      </svg>
    </div>
    
    <div class="ml-3 flex-1">
      <h3 class="text-sm font-medium text-red-800 dark:text-red-200">
        <%= error_title %>
      </h3>
      
      <div class="mt-2 text-sm text-red-700 dark:text-red-300">
        <p><%= error_message %></p>
        
        <% if show_details && error %>
          <details class="mt-3">
            <summary class="cursor-pointer text-red-600 dark:text-red-400 hover:text-red-800 dark:hover:text-red-200">
              기술적 세부사항 보기
            </summary>
            <div class="mt-2 p-3 bg-red-100 dark:bg-red-900/50 rounded border text-xs font-mono">
              <div class="mb-2">
                <strong>오류 ID:</strong> <%= error_id %>
              </div>
              <div class="mb-2">
                <strong>오류 유형:</strong> <%= error.class.name %>
              </div>
              <div class="mb-2">
                <strong>메시지:</strong> <%= error.message %>
              </div>
              <% if error.backtrace.present? %>
                <div>
                  <strong>스택 트레이스:</strong>
                  <pre class="mt-1 whitespace-pre-wrap break-all"><%= error.backtrace.first(5).join("\n") %></pre>
                </div>
              <% end %>
            </div>
          </details>
        <% end %>
      </div>
      
      <% if should_show_retry? %>
        <div class="mt-4">
          <%= link_to retry_action, 
                      class: "inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-red-700 bg-red-100 hover:bg-red-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 dark:text-red-200 dark:bg-red-800 dark:hover:bg-red-700",
                      'aria-describedby': "error-#{error_id}" %>
            <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
            </svg>
            다시 시도
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>